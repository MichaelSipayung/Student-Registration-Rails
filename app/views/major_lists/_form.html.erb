<%= form_with(model: major_list) do |form| %>
  <% if major_list.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(major_list.errors.count, "error") %> prohibited this major_list from being saved:</h2>

      <ul>
        <% major_list.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :jurusan, style: "display: block" %>
    <%= form.text_field :jurusan %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>

<%= form_with(model: language_degree_list) do |form| %>
  <% if language_degree_list.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(language_degree_list.errors.count, "error") %> prohibited this language_degree_list from being saved:</h2>

      <ul>
        <% language_degree_list.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :tingkat, style: "display: block" %>
    <%= form.text_field :tingkat %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>

<%= form_with(model: parent_job_list) do |form| %>
  <% if parent_job_list.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(parent_job_list.errors.count, "error") %> prohibited this parent_job_list from being saved:</h2>

      <ul>
        <% parent_job_list.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :pekerjaan, style: "display: block" %>
    <%= form.text_field :pekerjaan %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>

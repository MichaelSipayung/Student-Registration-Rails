<%= form_with(model: language_name_list) do |form| %>
  <% if language_name_list.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(language_name_list.errors.count, "error") %> prohibited this language_name_list from being saved:</h2>

      <ul>
        <% language_name_list.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :bahasa, style: "display: block" %>
    <%= form.text_field :bahasa %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
